services:
  mekongtunnel:
    build: .
    container_name: mekongtunnel-node
    restart: unless-stopped
    environment:
      - PORT=8080
      - DOMAIN=${DOMAIN:-localhost}
    ports:
      - "8080:8080"
