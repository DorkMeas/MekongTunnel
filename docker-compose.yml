services:
  mekongtunnel:
    build: .
    container_name: mekongtunnel-node
    restart: unless-stopped
    environment:
      - PORT=8080
      - DOMAIN=${DOMAIN:-localhost}
      - DATA_FILE=/app/data/tunnels.json
    volumes:
      - ./data:/app/data
    ports:
      - "8080:8080"
